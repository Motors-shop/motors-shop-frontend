import styled from "styled-components";

export const RecoveryContainer = styled.div`
  width: 100%;
  height: 100%;
`;

export const RecoveryContent = styled.main`
  min-height: 90vh;
  padding: calc(2vh + 2vw) calc(2.2vh + 2.2vw);

  display: grid;
  place-items: center;

  background-color: var(--grey8);
`;
