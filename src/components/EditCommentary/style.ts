import styled from "styled-components";

export const ThemeEditComment = styled.div`
  > form {
    display: flex;
    flex-direction: column;
    gap: 25px;
  }
`;

export const ThemeButtonContainer = styled.div`
  display: flex;
  gap: 20px;

  > button {
    width: 50%;
  }
`;
